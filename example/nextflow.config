params {
    samplesheet = "samplesheet.csv"

    benchmark_hvgs = 100
    scshc = false
    entropy = false
    cell_cycle = true
    leiden_resolutions = [0.5, 1]

    celltypist_model = "Cells_Intestinal_Tract.pkl"
    integration_methods = ["scvi", "scanvi", "harmony", "scanorama", "desc", "combat"]

    max_cpus = 20
    max_memory = "100G"
    max_time = "6.h"
}